# runtime data written by the kit (manifests, approvals, labelled files, registry) — local only
data/
# generator backups
workflows/*.bak*
.DS_Store
__pycache__/

# local database password and the n8n credential file built from it
.env
secrets/
